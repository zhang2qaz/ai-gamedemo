# 纽约行程 · 发票开销台账

管理纽约行程的所有发票、小票和报销单据：拍照发给 Claude → 自动识别、归类、入账 → 网页/接口实时查询。

## 使用流程

1. **拍照上传**：把发票、小票、报销单一张一张拍照发给 Claude（一次多张也可以）。
2. **自动入账**：Claude 识别票面上的商家、日期、金额、税费、小费、支付方式等信息，自动归类后写入台账，照片原件同时归档。
3. **实时查询**：
   - 网页：打开 `/expenses`，支持关键词搜索、按分类/报销状态/日期区间筛选，点任意记录看票据详情和原件照片；
   - 接口：`GET /api/expenses`，参数见下文；
   - 直接问 Claude：例如"这周吃饭花了多少""还有哪些没报销"。

## 分类标准

| 分类 key | 名称 | 覆盖范围 |
| --- | --- | --- |
| `dining` | 🍽️ 餐饮 | 正餐、咖啡、外卖、酒水 |
| `transport` | 🚕 交通 | 机票、出租车/网约车、地铁、火车 |
| `lodging` | 🏨 住宿 | 酒店、民宿及其税费 |
| `shopping` | 🛍️ 购物 | 商品零售、超市、伴手礼 |
| `entertainment` | 🎭 娱乐 | 演出、展览、景点门票 |
| `telecom` | 📱 通讯 | 电话卡、流量、上网 |
| `medical` | 💊 医疗 | 药品、诊疗 |
| `office` | 💼 办公会务 | 会议、打印、办公用品 |
| `fees` | 🧾 手续费杂费 | 银行手续费、小额杂费 |
| `other` | 📦 其他 | 无法归入上述分类的支出 |

## 报销状态

`pending 待报销` → `submitted 已提交` → `reimbursed 已报销`；纯个人消费标为 `personal 个人支出`。
新入账的单据默认 `pending`，需要变更状态时直接告诉 Claude（如"星巴克那笔已经报了"）。

## 数据存放

| 位置 | 内容 |
| --- | --- |
| `data/expenses/records.json` | 台账本体（结构见 `lib/expenses/types.ts` 的 `ExpenseLedger`） |
| `public/receipts/` | 票据照片原件。单票单拍命名 `<记录编号>.jpg`；一张照片拍多张小票时按批次原名归档（如 `IMG_3977.jpeg`），由多条记录共同引用 |
| `data/expenses/statements/` | 银行流水/账单归档与对账进展（`README.md`）。不在 `public/` 下，网页不对外提供 |

## 录入规范（Claude 入账时遵守）

- **编号**：`YYYYMMDD-NNN`，按票面消费日期 + 当日序号，如 `20260712-001`。
- **金额**：以票面实付金额为准（含税、含小费）；`subtotal`/`tax`/`tip` 可辨认时单独记录。
- **币种**：ISO 4217 代码（美国消费为 `USD`）。
- **日期时间**：以票面为准；照片模糊、信息缺失时在 `notes` 里注明存疑项，宁缺勿猜。
- **照片**：原件存入 `public/receipts/`，记录的 `receipts` 字段写站内路径（如 `/receipts/20260712-001.jpg`）。
- 每次入账后提交并推送，网页端即为最新数据。

## 银行流水对账

银行流水截图发给 Claude 后，按"日期 + 金额 + 卡尾号 + 商户名"逐笔比对台账：

| 比对结果 | 处理方式 |
| --- | --- |
| 核对一致 | 记录标记 `bankVerified: true`，列表显示 ✓ 已对账 |
| 金额有出入 | 通常是小费差额：按流水金额更正 `amount`/`tip`，差异原因写入 `bankNote` |
| 流水有、台账无 | 线上消费等无小票支出：按流水补录记录，`source: "bank"`，列表显示 🏦 流水补录 |
| 台账有、流水无 | 可能刷了其他卡或延迟入账，在 `bankNote` 标注待查 |

搜索 `已对账` / `未对账` / `流水补录` 可直接筛出对应记录；入账时预判可能对不上的（如小费未含）会先写入 `bankNote` 作为重点核对项。

## 查询接口

`GET /api/expenses`，全部参数可选、可组合：

| 参数 | 说明 | 示例 |
| --- | --- | --- |
| `q` | 关键词（商家/摘要/明细/地点/备注/分类名） | `q=咖啡` |
| `category` | 分类 key | `category=dining` |
| `status` | 报销状态 key | `status=pending` |
| `from` / `to` | 消费日期区间（含），`YYYY-MM-DD` | `from=2026-07-10&to=2026-07-15` |

返回 `{ query, summary, records }`，其中 `summary` 含总额（分币种）、分类/逐日/报销状态小计。

示例：查询所有待报销的餐饮支出

```bash
curl 'http://localhost:3000/api/expenses?category=dining&status=pending'
```
